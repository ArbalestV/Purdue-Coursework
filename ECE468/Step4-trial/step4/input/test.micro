PROGRAM p
BEGIN

	INT a,b,c;
	
	FUNCTION VOID main()
	BEGIN
		--a := 20;
		--b := 30;
		--c := 40;

		--c := c + a*b;
		--b := b*b + a;
		--a := (b*a)/a;
		--a := (a+b)*(d+c);
		--c := c + a*b + a*b+c/a + 20;
		--t := (t+z+t+t/2.0+z/4.0+z/5.0+z/6.0+z/7.0);
		--c := 0 - a + b;
		--a := a + b;
		--b := a * c;
		--a := a + 10;		
		--b := b + a + 10;	
		--c := 0 - 10;	
		--z := 3.14159;
		--x := z/2.0;
		--y := z/y;
		--t := (x+y+z)/z;
		--t := t*t;
		--a := (a+b+c+d)/a;
		c := c + a*b + (a*b+c)/a + 20;
		--c := c + a*b + (a*b+c) + 20;
		--t := (t+z+t+t/2.0+z/4.0+z/5.0+z/6.0+z/7.0);
		--t := t+z+t+(t/2.0)+(z/4.0)+(z/5.0)+(z/6.0+z/7.0);
		--t := t+z+t+(t/2.0+z/4.0);
		--t := (t+z+t+t/2.0+z/4.0+z/5.0+z/6.0*7.0+z/7.0); --ask dr. kulkarni about this...........
		--t := (t+z+t*8.0+t/2.0);
		--t := t+z+t+t/2.0+z/4.0+z/5.0+z/6.0+z/7.0;
		--t := t+z+t+(t/2.0+z/4.0+z/5.0+z/6.0+z/7.0);	
		--t := t+z+t+(t/2.0+z/4.0+z/5.0+z/6.0+z/7.0);
		--a := (a+b)*(d+c)-(a+b+c+d)/a;
		--a := (a+b+c+d)/a;
		--a := (a+b)*(d+c);

		WRITE (c);
		WRITE (b);
		WRITE (a);
	
	END
END
