#include "lab2-api.h"

/*
this is my test for q5 trying to test sleep
*/

void main (int argc, char * argv[])
{
  char * totaltime;
  int time;
  totaltime = argv[1];
  //Printf(totaltime);Printf("\n");
  yield();
  time = 10000;
  Printf("Going to sleep\n");
  sleep(time);
  Printf("this process has hopefully awoken\n");
  
}
